using UnityEngine;

public class StopLineTrigger : MonoBehaviour
{
  // Reserved for future logic (e.g. traffic lights, yield enforcement)

  void OnTriggerEnter2D(Collider2D other)
  {
    // Placeholder: detect car arrival at stop line
  }
}
